import SwiftUI


struct ContentView: View {
    var body: some View {
        VStack {
            Image("cat")
                .imageScale(.small)
                .foregroundStyle(.tint)
            Text("Hello! We are trying to figure this out")
        }
        .padding()
    }
}


#Preview {
    ContentView()
}
